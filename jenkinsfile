pipeline {
    agent any

    stages {
        stage('Pull Code') {
            steps {
                echo '✅ Code pulled from GitHub'
            }
        }

        stage('Build') {
            steps {
                echo '🔨 Simulating build step...'
                sh 'ls -l'
            }
        }

        stage('Test') {
            steps {
                echo '🧪 Running tests...'
                sh 'echo "All tests passed ✅"'
            }
        }
    }
}

